<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.revia.lastdance.post.dao.PostMapper">

    <resultMap id="postResultMap" type="com.revia.lastdance.post.vo.PostVO">
        <id property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="postTitle" column="post_title"/>
        <result property="postContent" column="post_content"/>
        <result property="isNotice" column="is_notice"/>
        <result property="postViewCunt" column="post_view_cunt"/> <!-- postViewCunt 추가 -->
        <result property="createdDate" column="created_date"/>
        <result property="createdId" column="created_id"/>
        <result property="updatedDate" column="updated_date"/>
        <result property="updatedId" column="updated_id"/>
    </resultMap>

    <resultMap id="fileNestedResultMap" type="com.revia.lastdance.post.vo.FileVO">
        <id property="fileId" column="file_id"/>
        <result property="postId" column="post_id"/>
        <result property="uploadName" column="upload_name"/>
        <result property="fileType" column="file_type"/>
        <result property="imgUrl" column="img_url" jdbcType="BLOB"/>
    </resultMap>

    <resultMap id="postWithFilesResultMap" type="com.revia.lastdance.post.vo.PostVO" extends="postResultMap">
        <collection property="files" ofType="com.revia.lastdance.post.vo.FileVO" resultMap="fileNestedResultMap"/>
    </resultMap>

    <!-- 기존 selectAllPostsPaged는 일반 게시물만 조회하도록 변경 -->
    <select id="selectAllPostsPaged" resultMap="postWithFilesResultMap">
        SELECT
            p.post_id,
            p.user_id,
            p.post_title,
            p.post_content,
            p.is_notice,
            p.post_view_cunt, <!-- postViewCunt 추가 -->
            p.created_date,
            p.created_id,
            p.updated_date,
            p.updated_id,
            f.file_id,
            f.upload_name,
            f.file_type,
            f.img_url
        FROM tb_post p
        LEFT JOIN tb_file f ON p.post_id = f.post_id
        WHERE p.is_notice = FALSE
        ORDER BY p.created_date DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 기존 countAllPosts는 일반 게시물만 카운트하도록 변경 -->
    <select id="countAllPosts" resultType="int">
        SELECT COUNT(*) FROM tb_post WHERE is_notice = FALSE
    </select>

    <delete id="deletePost">
        DELETE FROM tb_post WHERE post_id = #{postId}
    </delete>

    <select id="selectPostById" resultMap="postWithFilesResultMap">
        SELECT
            p.post_id,
            p.user_id,
            p.post_title,
            p.post_content,
            p.is_notice,
            p.post_view_cunt, <!-- postViewCunt 추가 -->
            p.created_date,
            p.created_id,
            p.updated_date,
            p.updated_id,
            f.file_id,
            f.upload_name,
            f.file_type,
            f.img_url
        FROM tb_post p
        LEFT JOIN tb_file f ON p.post_id = f.post_id
        WHERE p.post_id = #{postId}
    </select>

    <insert id="insertPost" parameterType="com.revia.lastdance.post.vo.PostVO" useGeneratedKeys="true" keyProperty="postId">
        INSERT INTO tb_post
        <trim prefix="(" suffix=")" suffixOverrides=",">
            post_title,
            post_content,
            is_notice,
            post_view_cunt, <!-- postViewCunt 추가 -->
            created_date,
            user_id,
            <if test="createdId != null">created_id,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            #{postTitle},
            #{postContent},
            #{isNotice},
            0, <!-- 초기 조회수 0으로 설정 -->
            CURRENT_TIMESTAMP,
            #{userId},
            <if test="createdId != null">#{createdId},</if>
        </trim>
    </insert>

    <update id="updatePost" parameterType="com.revia.lastdance.post.vo.PostVO">
        UPDATE tb_post
        SET
            post_title = #{postTitle},
            post_content = #{postContent},
            is_notice = #{isNotice},
            updated_date = CURRENT_TIMESTAMP,
            user_id = #{userId},
            <if test="createdId != null">created_id = #{createdId},</if>
            updated_id = #{updatedId}
        WHERE post_id = #{postId}
    </update>

    <!-- 조회수 증가 -->
    <update id="incrementPostViewCunt">
        UPDATE tb_post
        SET
            post_view_cunt = post_view_cunt + 1
        WHERE post_id = #{postId}
    </update>

    <!-- New: 상위 N개 공지사항 조회 -->
    <select id="selectTopNotices" resultMap="postWithFilesResultMap">
        SELECT
            p.post_id,
            p.user_id,
            p.post_title,
            p.post_content,
            p.is_notice,
            p.post_view_cunt, <!-- postViewCunt 추가 -->
            p.created_date,
            p.created_id,
            p.updated_date,
            p.updated_id,
            f.file_id,
            f.upload_name,
            f.file_type,
            f.img_url
        FROM tb_post p
        LEFT JOIN tb_file f ON p.post_id = f.post_id
        WHERE p.is_notice = TRUE
        ORDER BY p.created_date DESC
        LIMIT #{count}
    </select>

    <!-- New: 일반 게시물만 페이지네이션하여 조회 (기존 selectAllPostsPaged와 동일한 로직이나 is_notice 조건 추가) -->
    <select id="selectAllGeneralPostsPaged" resultMap="postWithFilesResultMap">
        SELECT
            p.post_id,
            p.user_id,
            p.post_title,
            p.post_content,
            p.is_notice,
            p.post_view_cunt, <!-- postViewCunt 추가 -->
            p.created_date,
            p.created_id,
            p.updated_date,
            p.updated_id,
            f.file_id,
            f.upload_name,
            f.file_type,
            f.img_url
        FROM tb_post p
        LEFT JOIN tb_file f ON p.post_id = f.post_id
        WHERE p.is_notice = FALSE
        ORDER BY p.created_date DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- New: 일반 게시물만 카운트 -->
    <select id="countAllGeneralPosts" resultType="int">
        SELECT COUNT(*) FROM tb_post WHERE is_notice = FALSE
    </select>

    <!-- New: 게시물 검색 -->
    <select id="searchPosts" resultMap="postWithFilesResultMap">
        SELECT
            p.post_id,
            p.user_id,
            p.post_title,
            p.post_content,
            p.is_notice,
            p.post_view_cunt, <!-- postViewCunt 추가 -->
            p.created_date,
            p.created_id,
            p.updated_date,
            p.updated_id,
            f.file_id,
            f.upload_name,
            f.file_type,
            f.img_url
        FROM tb_post p
        LEFT JOIN tb_file f ON p.post_id = f.post_id
        <where>
            p.is_notice = FALSE
            <if test="searchType == 'title' and searchKeyword != null and searchKeyword != ''">
                AND p.post_title LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
            <if test="searchType == 'content' and searchKeyword != null and searchKeyword != ''">
                AND p.post_content LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
            <if test="searchType == 'title_content' and searchKeyword != null and searchKeyword != ''">
                AND (p.post_title LIKE CONCAT('%', #{searchKeyword}, '%') OR p.post_content LIKE CONCAT('%', #{searchKeyword}, '%'))
            </if>
            <if test="searchType == 'userId' and searchKeyword != null and searchKeyword != ''">
                AND p.user_id LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
        </where>
        ORDER BY p.created_date DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- New: 검색된 게시물 카운트 -->
    <select id="countSearchPosts" resultType="int">
        SELECT COUNT(*)
        FROM tb_post p
        <where>
            p.is_notice = FALSE
            <if test="searchType == 'title' and searchKeyword != null and searchKeyword != ''">
                AND p.post_title LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
            <if test="searchType == 'content' and searchKeyword != null and searchKeyword != ''">
                AND p.post_content LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
            <if test="searchType == 'title_content' and searchKeyword != null and searchKeyword != ''">
                AND (p.post_title LIKE CONCAT('%', #{searchKeyword}, '%') OR p.post_content LIKE CONCAT('%', #{searchKeyword}, '%'))
            </if>
            <if test="searchType == 'userId' and searchKeyword != null and searchKeyword != ''">
                AND p.user_id LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
        </where>
    </select>

</mapper>
