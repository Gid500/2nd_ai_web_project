<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.revia.lastdance.withdrawal.WithdrawalDAO">
    <delete id="deleteUser">
        DELETE FROM tb_user
        WHERE user_id = #{userId}
    </delete>
</mapper>
